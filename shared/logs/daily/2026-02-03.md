# Sesja 2026-02-03

## Podsumowanie

### 1. Konfiguracja GitHub
- Zainstalowano `gh` CLI przez `apt`
- Zainstalowano `git`
- Zalogowano jako `numerika-ai`
- Skonfigurowano git user: `numerika-ai@users.noreply.github.com`

### 2. Tank Operator - Konfiguracja Claude Code
- Sklonowano repo: `https://github.com/numerika-ai/tank-operator`
- Lokalizacja: `~/tank-operator/` (symlink: `~/linux-orchestrator`)

#### Struktura:
```
~/tank-operator/
├── claude-code-tank-operator/    # Konfiguracja Claude Code
│   └── CLAUDE.md                 # Zasady zarządzania systemem
├── clawdbot-tank-operator/       # Konfiguracja Clawdbot
│   └── CLAWDBOT.md
├── shared/                       # Wspólne zasoby CC + CB
│   ├── CHANGELOG.md              # Główny log
│   ├── logs/
│   ├── registry/
│   │   ├── DOCKER-REGISTRY.md
│   │   ├── MODEL-REGISTRY.md
│   │   └── INSTALL-HISTORY.md
│   └── state/
│       ├── SYSTEM-STATE.md
│       └── ACTIVE-SESSIONS.md
└── infrastructure/
```

#### Kluczowe zasady zsynchronizowane:
- Format logów: `[YYYY-MM-DD HH:MM] [SOURCE] [TAG] [STATUS] opis | szczegóły`
- SOURCE: `[CC]` = Claude Code, `[CB]` = Clawdbot
- Wszystkie logi do `shared/` (nie lokalnie)
- Wspólne registry dla obu systemów

### 3. Konfiguracja ~/.claude/settings.json
- Dozwolone: docker, git, system info commands
- Zabronione: rm -rf /, chmod 777, secrets, iptables -F
- Hooki: ostrzeżenia dla rm, docker rm, firewall
- Post-hooki: auto-logowanie docker operations do `shared/logs/`

### 4. Push na GitHub
- Commit: `ecf070b` - log: [CC] Initial Claude Code configuration
- Repo zaktualizowane

### 5. Docker
- Zainstalowano Docker `29.2.1`
- Docker Compose `v5.0.2`
- Problem: brak uprawnień (wymaga przelogowania po `usermod -aG docker`)

### 6. OpenClaw (w trakcie)
- Sklonowano: `~/openclaw-install/`
- Instalacja przez Docker (`docker-setup.sh`)
- Status: **OCZEKUJE** na przelogowanie użytkownika (uprawnienia docker)

## Następne kroki
1. Przelogować się (uprawnienia docker)
2. Uruchomić: `cd ~/openclaw-install && ./docker-setup.sh`
3. Skonfigurować kanały (Discord/Telegram/WhatsApp)
4. Połączyć OpenClaw z tank-operator/clawdbot-tank-operator

## Pliki konfiguracyjne
- `~/.claude/settings.json` - hooki Claude Code
- `~/tank-operator/claude-code-tank-operator/CLAUDE.md` - zasady
- `~/tank-operator/clawdbot-tank-operator/CLAWDBOT.md` - zasady Clawdbot

## Komendy do zapamiętania
```bash
# Praca z Claude Code
cd ~/tank-operator/claude-code-tank-operator

# Logi
cat ~/tank-operator/shared/CHANGELOG.md

# Push na GitHub
cd ~/tank-operator && git add shared/ && git commit -m "log: [CC] opis" && git push

# OpenClaw (po przelogowaniu)
cd ~/openclaw-install && ./docker-setup.sh

# Dashboard OpenClaw
http://127.0.0.1:18789/
```
